$(function () {
    // 给所有表单添加验证
    $(':text, textarea').each(function () {
        $(this).validate();
    });
    // 按钮点击后 会被禁用
    $('.btn').btnDisabled();
    // 单选按钮被选中时 激活对应的文件框
    $('form').radioToggle();
    // 添加模块的显示隐藏事件
    $().showOrHide();
});